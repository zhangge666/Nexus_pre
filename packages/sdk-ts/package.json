{
  "name": "@nexus/sdk-ts",
  "version": "0.1.0",
  "description": "Nexus Memory Protocol 的公开 TypeScript SDK。",
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "pnpm --filter @nexus/protocol-client build && tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@nexus/protocol-client": "workspace:*"
  }
}
